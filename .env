# Rate Limiter Configuration
IP_REQUESTS_PER_SECOND=5
IP_BLOCK_DURATION_SECONDS=300  # 5 minutes

# Default token configuration (can be overridden per token)
DEFAULT_TOKEN_REQUESTS_PER_SECOND=10
DEFAULT_TOKEN_BLOCK_DURATION_SECONDS=300  # 5 minutes

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Server Configuration
SERVER_PORT=8080
